order = 3
while order > 0 :                   # order <= 0이 되면 반복 멈춤
    print('%d 통닭 튀긴다' % order)     # 들여쓰기: 블록 시작
    order = order - 1                   # order값 1 감소
print('3마리 다 튀김')              # 일반코드
