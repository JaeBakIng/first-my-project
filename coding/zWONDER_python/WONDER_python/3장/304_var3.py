var = '100'
print( var * 2 )    # 문자 * 2는 문자 2번 출력 문자

var = int(var)      # int()는 정수로 바꾸는 함수
print( var * 2 )

var = float(var)    # float()는 실수로 바꾸는 함수
print( var * 2 )

var = str(var)      # str()은 문자로 바꾸는 함수
print( var * 2 )
